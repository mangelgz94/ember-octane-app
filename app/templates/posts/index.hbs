{{#each posts as |post|}}
  <LinkTo @route="posts.show" @model={{post.id}}><h3>Title: {{post.title}}</h3></LinkTo>
  <p>{{post.body}}</p>
  <LinkTo @route="posts.user" @model={{post.user.id}}>By: {{post.user.name}}</LinkTo><br>
{{/each}}